# Habit learning build

- [x] Web reference and shared fixtures: daily cap, confidence, compatible
  history and receipt evidence. Native implementations match the same cases.
- [ ] Final verification: Go/web suites, Android full build and instrumentation,
  iOS full core/UI gate; actual Home selection, explicit correction, reload,
  both phone sizes/schemes, relevant visual matrix and shell upgrade.
- [ ] Release: 1.7.1, native build 8, worker v65. Signed Android download,
  TestFlight upload, commit/push, image deploy and public byte checks.
- [ ] Close: keep current contracts, remove this folder after verified release.
