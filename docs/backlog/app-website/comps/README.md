# Selected website comp — R9 A

Owner selected A, requested the green Sydney dot and Sleepers beside the
header wordmark, then rejected the detached Live Activity card. This prototype
shows the complete real iPhone system screen beside the full in-app tracker;
phones stack on mobile. Serve this folder as the HTTP root. Routes are
normalized to `/`, `/privacy/`, `/support/`; `/#follow` opens the tracker.

The eight WebP app screenshots retain full 1206×2622 proportions. The two F1
tracker assets are fresh native captures; the other six keep their earlier bytes. The two `ios-activity-phone-*.png` files are identical complete
native system captures. `native-live-activity/` records source, fixture,
timetable and simulator limitations. Sleepers derives from the canonical
`assets/brand/sleepers.svg`. Standalone card crops and Kellyville are rejected.

The F1 in-app screenshot was retaken in both schemes from the current native
renderer. The travelled segment is correctly dimmed; `native-f1-tracker/`
records the capture, fixed example clock and lossless asset verification.
Use these new tracker bytes when building.

These are selected prototype materials, not a deployed website or completed
production verification. Build against the current site calibration and
`build_plan.md`.
