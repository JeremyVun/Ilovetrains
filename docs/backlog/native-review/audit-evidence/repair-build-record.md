# Native review: approved repair build

Owner authorized implementation on 2026-09-07: “For the rest, if the fix is obvious, it improves the app, then do so and make sure ios, android, and web are aligned.” Non-obvious choices need explanation and visual evidence where applicable. This plan replaces the obsolete blanket 63-entry phases.

## Binding owner rulings

- D1/D2: Android follows iOS.
- D3: Android follows iOS, including spacing and text alignment.
- D5: both use web Now handling; Android trip-row spacing follows iOS.
- D8: fix Android blue overhang.
- D4: owner resolved the remaining question on 2026-09-08: “countdowns must always appear. if web doesnt show, then web needs to be fixed”. Keep future numerical figures on all three clients through stale/offline/retained states, with unchanged freshness indicators and existing Now/minutes/rounded-hours formatting. Past Ago and cancellation dash remain. Root owns native formatter/tests/contracts; integration agent owns web formatter/tests and service-worker bump; visual agent owns final captures.
- C9: port web Just added to both native clients.
- C10: owner sees no clear problem. Keep existing display depth/footer policy and Now landing; no longer-board redesign.
- D6/group 9: preserve unsent drafts across navigation. Apply the established web/iOS field-focus treatment as an obvious parity repair.
- The rejected A/B/C comps, new coverage composition, large H and compressed seven-row board remain rejected.

## Scope and ownership

These are file-disjoint build scopes, not overlapping platform worktrees. Existing unrelated dirty files are preserved. No .env may be read directly or indirectly. Models follow the owner's AGENTS defaults (mechanical engines: terra; UI and reviews: sol), overriding obsolete unavailable models in the backlog skill.

1. **Data engines — done.** A1–A5 and narrowly safe A6 cleanup/G3 tests. Own both OfflineRealtime, OfflinePlanner, OfflinePackageStore/OfflineZip and their tests. Prove through-service skipped stops, older headers, expiry/future tolerance, malformed metadata and failed candidate open rollback. Preserve static schedule immutability and update validation; no startup-marker/streaming/router optimization. Do not edit view models, UI, prediction, storage or contracts.
2. **Android presentation/controller — implemented; final visual verification active.** B3–B9, C2–C5/C7–C9/C11, D1–D3/D5–D8, E12-equivalent refresh safeguards, F1/F3/F5/F7 and safe F6 travel-order preservation. Own Ui*.kt, TrainViewModel.kt, Models.kt, TransitApi.kt, BoardRetention.kt, controller/presentation tests and new Android UI helpers. Port actual references; only one smoke drive after unit gates. Do not edit data-engine files, Prediction.kt, Storage.kt, contracts, screenshot instruments or baselines. Just added/nearest and draft need controller state; cross-platform source/identity semantics follow audit. Root owns pending retained figure policy.
3. **iOS presentation/controller/storage — implemented; final visual verification active.** B2–B5/B7, C1/C3/C4/C7–C9/C11, D5/D6 parity, E2/E3/E12, F1/F3/F4/F5/F7/F8/F10 and safe F6 travel-order preservation. Own iOS UI, TrainViewModel, Prediction, AppState, DeviceStore, TransitModels, TransitAPI and corresponding tests. Preserve unrelated existing SetupView changes. Do not edit data-engine files, contracts, baselines, Xcode project or screenshot instruments without coordinating. Root owns pending retained figure policy. No broad performance refactor.
4. **Root correctness/docs — active.** Android B1, E2 where coordinated, F8-equivalent persisted shape checks if needed, audit/contract decisions, integration and verification orchestration. Own Android Prediction.kt/Storage.kt and their tests. Leave cache eviction policy F9 unchanged pending a clear benefit/ruling. No new saved-line schema/fallback color; fix proven ordering only.
5. **Verification wave — active.** After builders finish, inspect actual diffs, run appropriate native/unit gates, perform cross-platform real-client matrix at 390×844/412×732 in both schemes, and inspect affected source/focus/setup/feedback/retained states. Use tools/visual-regression.js as documented. Verify geometry against the actual web and iOS references, including Now, small H, two changes, cancellation and native row spacing. Repair failures before accepting baselines. Run Go/web gates for shared contracts/fixtures and any web change; bump SW VERSION with any cached web file change.
6. **Closeout — pending.** Record built behavior, migrate unresolved non-obvious items to their own explicit backlog design scopes, update durable contracts/exemplars, remove completed disposable backlog materials only after the remaining decisions and verification are accounted for. Follow native/deploy runbooks; never silently deploy unrelated dirty work.

## Held choices

C10 future display/footer policy, C6 indicator/coverage composition, F6 saved-line schema/new fallback color, F9 Android cache eviction, B10/B11 unproven permission lifecycle, E1 and E4–E11 speculative optimizations. Some may be dropped when their claim is unproven rather than carried as work. They are not included by implication in the obvious-fix instruction. D4 is fully authorized by the subsequent always-visible ruling.

F6 clarification from source inspection: web renders names and an arrow without a badge or coloured spine when a saved trip has no known lines. Its empty line-column container still reserves 15px plus a 13px gap. Both native clients remove their invented T badge and retain that empty gutter so station names align with known-line trips. This does not require a new schema or a new fallback color.

## Verification and completion record

Pending. Audit probes in audit-evidence assert old defects and are evidence, not permanent desired-behavior tests. New tests must fail the old implementation, and resulting app behavior needs real-client verification. No phase is done merely because it compiles.

Integration review on 2026-09-08 identified these repairs within the approved scope:

- B8: a failed realtime match for the focused service must retain that service's old observations while retaining a successful independent alternatives refresh. Tapping the focused service must use its own board even if the alternative plan contains the same key. Identity and generation guards run again after the alternative plan suspends.
- D6: feedback message and category editing pause while submission is in flight, matching web. Otherwise success can erase text typed after Send. Navigation remains available and failures preserve the submitted draft.
- B3: Change destination prefills the focused saved trip's directional origin, preserving its coordinates. API journey endpoints may have no coordinates and must not replace the known saved endpoint when the corrected trip is saved.
- C8: Next delegates figure semantics to the ordinary row formatter. A fresh `X-Data-Stale`/degraded response changes freshness indication but does not hide a still-current countdown on web. The initial native helper incorrectly suppressed it; that extra guard was removed, with tests preserving suppression for genuinely old/offline future rows. The actual web probe returned `stale:false`, `dot:stale`, `nextFigure:18` for a fresh degraded response.
- C4: independent source follow-up reproduced the same distinction for Running late in actual web `homeModel`. The native presentation predicate now permits recent degraded observations while excluding offline, expired, scheduled-only and retained journey evidence. `BoardData.isLive` and its stricter inference rules remain unchanged. Header/saved-row status and the pinned suffix have explicit tests.

Native test work uses a dedicated simulator and derived-data directory because other worktrees run suites on the shared iPhone. Overlapping test-process failures are not accepted as product findings or as successful gates; rerun exclusively on the dedicated device.

Integration Go gate passes; web has 342 passing tests (logs `/tmp/native-review-go-integration.log` and `/tmp/native-review-web-integration.log`). These cover the current shared worktree, including concurrent web work; that work is not part of this repair's ownership. Final native gates and visual evidence remain pending.

Past-service D4 regression was demonstrated before repair: `RowConformanceTest.pastFiguresKeepElapsedTimeWhenObservationsAreRetainedOrOffline` failed against the old stale branch (`/tmp/native-review-past-before.log`). Two existing retention assertions also encoded the rejected blank-figure/Last known behavior and now assert the five-minute elapsed figure while preserving the clock and source checks. The repaired Android full JVM gate passes 70 tests (`/tmp/native-review-past-after-final.log`); the corresponding iOS row regression passed in the subsequent 79-unit-test gate. This source change follows the builders' initial 69 Android and 78+5 iOS gates and requires rebuilt final native captures.

Subsequent device gates passed: Android OfflinePlanner instrumentation 5/5 (`/tmp/native-review-android-engine-instrumentation.log`); iOS 79 unit + 5 UI after the D4 repair; Android production-controller instrumentation 4/4 for draft navigation, inferred redirect coordinates in both directions, focused/alternative provenance and retained last-answer evidence. Controller XML is copied to `/tmp/native-review-build-20260907/controller-evidence/android-controller.xml`. C4/C8 presentation refinements followed these gates; final unit checks and affected captures must include those edits.

A8 documentation now describes the actual four service dates scanned, bounded by the existing planning horizon. A7 retains the current Circular Quay rail/ferry policy; the bundled package's modes do not establish a routing defect or authorize a hypothetical additional-mode rule.

The final iOS integrated gate (`/tmp/native-review-ios-final-stable-gate-3.log`) passed 78 of 79 unit tests and all five UI tests, including feedback-draft navigation without sending. Its single failure expected automatic setup selection after a fresh fix; concurrent, separately authorized accuracy-aware setup work can instead offer a station choice. The test now asserts the intended freshness invariant: Town Hall is the nearest accepted station and appears in nearby choices, and the controller clock reaches the newly received fix timestamp. That corrected test passed in `/tmp/native-review-ios-fresh-fix-targeted.log`; no product freshness guard was weakened. New tests subsequently added by the separate setup task are not included in these counts. An earlier missing Xcode file reference and Swift `isEmpty()` typo were concurrent setup integration errors, not native-review findings.

One Android 412×732 capture collided with an external worktree's instrumentation, crashed and produced missing files. It is retained as a failed capture, not a product verdict or passing gate. Capture dimensions also distinguish the configured Android display from the app-root bitmap, which excludes system bars; the comparison must not stretch that bitmap to the full display height.

Root visual inspection found C9 incomplete in the first iOS build: `JUST ADDED · NEVER RIDDEN` still used the generic uppercase label. The renderer now follows web: only `Just added` is 12pt italic, followed by distance alone in the ordinary metadata treatment. Android's distance no longer inherits the label's italics, and explicit trip selection clears its automatic-trip mark. The new Android controller regression exercises real location-driven creation followed by open/back. Rebuilds, that regression and affected Just added captures must include this final refinement; earlier C9 screenshots are before evidence, not accepted results.

The final Android controller suite now passes all five cases, including real automatic trip creation followed by explicit selection (`/tmp/native-review-android-controller-final.log`, 10.642 seconds). The final iOS C9 and observed-delay retained captures were rebuilt and inspected in both schemes at 402×874 and true 390×844. They show italic `Just added` with distance only, and retained 22:54/22:48 with 5 min Ago plus the unchanged last-updated indicator.

The Android size mismatch was traced to the new screenshot test tag being after safe-area padding. Moving only that semantics tag before the padding restores full 1024×2216 frames without moving product content. The earlier inner-content captures remain valid layout observations but cannot establish a full-frame pixel baseline match. A bounded, logged retry handles only the intermittent PixelCopy timeout; no image or comparison threshold is substituted.

The 412×732/font1.3 inspection found a real remaining wrap: the transfer time-column label split `CANCELLED` across lines. It now measures and fits the label inside its unchanged 69dp column; default sizing remains unchanged. A rendered single-line/no-overflow assertion and after capture are required before marking this verified. Concurrent, separately owned native toy source briefly broke the next build; final capture work must use matching preserved APKs or an isolated source snapshot, not assume mutable build outputs still correspond to a prior passing run.

The owner resolved the D4 future-row question on 2026-09-08: countdowns always appear, including on web. Earlier verification notes describing intentional blank future figures are superseded. The formatter now keeps Now/minutes/rounded-hours through offline, aged, retained, schedule-only and missing-source states; the same formatter feeds Next. Native stale provenance follows web's scheduled/delay treatment instead of replacing the figure with Last known. Freshness and inference predicates are unchanged. Final source-state regression tests and real-client captures must include this last ruling.
